name: Deploy CRM dev

on:
  push:
    branches: ["crm-dev"]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout source
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Setup SSH
      - name: Prepare SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_STAGING_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.EC2_STAGING_HOST }} >> ~/.ssh/known_hosts

      # 3. Version tagging (simple)
      - name: Auto Version Tagging
        id: tag
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.EC2_STAGING_USER }}@${{ secrets.EC2_STAGING_HOST }} << 'EOF'
            echo "---- Navigating to project directory ----"
            cd /home/ubuntu/crms-backend

            echo "---- Pulling latest code from crm branch ----"
            git fetch origin crm-dev
            git reset --hard origin/crm-dev

            echo "---- Applying dev environment variables ----"
            cp .env.dev .env.dev

            echo "---- Installing dependencies ----"
            npm install --dev

            echo "---- Reloading PM2 process (zero downtime) ----"
            pm2 reload crms-dev --update-env

            echo "---- Running Health Check ----"
            for i in {1..5}; do
              STATUS=$(curl -k -s -o /dev/null -w "%{http_code}" https://crms-api-dev.duckdns.org/v2/utility/health)
              echo "Health Try $i = $STATUS"
              [ "$STATUS" = "200" ] && break
              sleep 5
            done

            if [ "$STATUS" != "200" ]; then
              echo "Health check FAILED — rolling back..."
              git reset --hard "$PREV_COMMIT"
              npm install --dev
              pm2 restart crms-dev --update-env
              exit 1
            fi

            echo "---- Deployment Success ----"
          EOF
